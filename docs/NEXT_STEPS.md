# Next Steps

Required Android implementation and runtime verification are complete.

1. On `feature/visual-polish`, manually verify Bonus #1 phase 1 (Profile Setup + Chat shell + message bubbles/states) in light/dark, then continue Bonus #1 phase 2 only after that (entry/composer/media-grid polish — not started).
2. Finish evaluator/submission documentation (root README + backend/workflow accuracy) on the appropriate docs branch if not already merged, then promote through `development` → `staging` → `master` as usual.
3. Before GitHub submission, confirm repository visibility is **Public**.
4. Remaining bonus product work (after visual polish):
   - FCM push notifications (separate from the existing WorkManager send notification)
   - audio / voice messages
   - instrumentation / UI tests
5. Optional product surfaces already noted as out of required scope (full-screen media viewer, in-bubble video playback, download/save) remain non-mandatory unless promoted explicitly.
